# Cập nhật tạo prompt miễn phí

- Tạo chủ đề, bối cảnh, timeline và prompt bằng engine quy tắc chạy trên máy chủ.
- Không gọi Gemini hoặc Vertex AI; không cần GEMINI_API_KEY.
- Giữ nguyên kiểm tra gói dùng thử/gói đã mua, SePay và quản trị.
- Cài đặt lại dependencies rồi build/deploy như phiên bản hiện tại.

Lưu ý: Engine miễn phí không phân tích trực tiếp nội dung hình ảnh/video bằng mô hình AI. Chất lượng đầu ra phụ thuộc vào phần tóm tắt và mô tả phong cách người dùng nhập.
