# Cập nhật tạo prompt miễn phí

- Tạo chiến dịch, prompt ảnh, prompt video, voiceover và phụ đề bằng engine cục bộ.
- Không gọi Gemini hoặc Vertex AI; không cần GEMINI_API_KEY.
- Nút gợi ý người dẫn và tinh chỉnh shot đã chuyển sang xử lý quy tắc miễn phí.
- Giữ nguyên kiểm tra quyền sử dụng, dùng thử, SePay và quản trị.

Lưu ý: ảnh tải lên vẫn được giữ trong giao diện nhưng engine miễn phí không dùng mô hình thị giác để nhận diện ảnh. Người dùng nên mô tả rõ đặc điểm sản phẩm để prompt chính xác hơn.
